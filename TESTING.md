# G3M Battery 测试约定

本文说明 G3M Battery 的自动化测试、Windows 构建检查和真实设备手工验收方式。项目只支持 Windows，因此带有 `//go:build windows` 的测试应在 Windows runner 或 Windows 开发机上执行。

## 自动检查

在项目根目录执行：

```text
gofmt -l cmd/g3m-battery
git diff --check
go test ./...
go vet ./...
```

自动化测试覆盖不依赖真实设备的协议解析、状态映射、历史采样、异常事件、会话边界和续航估算。HID 枚举、Win32 托盘窗口和真实设备响应不在单元测试中伪造，应通过 Windows 手工验收确认。

## 真实设备验收

- USB 有线连接时，电量、连接方式、充电中和已充满状态一致；
- 2.4G 接收器连接时，电量和连接方式显示一致；
- 同时插入有线和接收器时，有线优先；有线读取失败后能尝试接收器；
- 拔出设备、重新插入设备和读取失败恢复时，不产生重复历史事件；
- 低于 20% 且非充电状态只提示一次，恢复到阈值以上后再次低于阈值可以重新提示；
- 历史窗口可以打开，24 小时和 7 天曲线可以切换，未观测区间不会被连线；
- 充电时长、100% 到 20% 放电观测和“暂无估算”条件符合预期；
- 关闭程序后立即重新启动，历史数据不能把未运行期间算入充电、放电或续航估算；
- 移动 exe 后，开机启动菜单不会继续错误显示旧路径仍有效；
- 右键菜单中的刷新、历史窗口、开机启动和退出操作正常。
